import { Hero, Nav, TipList } from '../../components';

export const AllTopicsPage = () => {
    return (
        <section className = 'layout'>
            <Nav />
            <Hero />
            <TipList />
        </section>
    );
};
